<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Home</title>
  </head>
  <body>
    <h1>Welcome to the home page!</h1>
    <% if (user) { %>
      <p>You are currently logged in as <%= user.name %> (<%= user.email %>)</p>
      <a href="/profile">Go to your profile</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
    <% } %>
  </body>
</html>
